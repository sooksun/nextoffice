node_modules
.next
dist
.git
.env
.env.*
# .env.production is NOT whitelisted: it holds secrets (DB/JWT/MinIO) and is
# only needed at runtime via docker-compose `env_file`, never at build time.
*.md
!apps/web/AGENTS.md
docs/
*.pdf
docs/*.jpg
docs/*.png
!apps/web/public/**
